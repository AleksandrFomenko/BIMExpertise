using System.Windows.Threading;

namespace Duct.ViewModels;

public sealed class DuctViewModel : ObservableObject
{
    public DuctViewModel(Document doc, Dispatcher viewDispatcher)
    {
        
    }
}