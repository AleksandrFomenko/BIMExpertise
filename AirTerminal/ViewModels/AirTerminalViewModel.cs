using System.Windows.Threading;

namespace AirTerminal.ViewModels;

public sealed class AirTerminalViewModel : ObservableObject
{
    public AirTerminalViewModel(Document doc, Dispatcher viewDispatcher)
    {
       
    }
}