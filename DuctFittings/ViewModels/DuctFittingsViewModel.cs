using System.Windows.Threading;

namespace DuctFittings.ViewModels;

public sealed class DuctFittingsViewModel : ObservableObject
{
    public DuctFittingsViewModel(Document doc, Dispatcher viewDispatcher)
    {
       
    }
}