using System.Windows.Threading;

namespace PipeInsulation.ViewModels;

public sealed class PipeInsulationViewModel : ObservableObject
{
    public PipeInsulationViewModel(Document doc, Dispatcher viewDispatcher)
    {
        
    }
}